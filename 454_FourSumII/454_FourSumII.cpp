#include <iostream>
#include <vector>

using namespace std;

class Solution {
public:
	int fourSumCount(vector<int>& A, vector<int>& B, vector<int>& C, vector<int>& D) {
		const int a = A.size(), b = B.size(), c = C.size(), d = D.size();

	}
};

int main(int argc, char *argv[]){
	Solution s;
	cout << s.fourSumCount(vector<int>{1, 2}, vector<int>{-2, -1}, vector<int>{-1, 2}, vector<int>{0, 2});
	system("pause");
	return 0;
}